
# @package _global_
# Experiment: DKL-BO vs random search over many seeds
defaults:
  - override /data: c2db_gap
  - override /model: cgcnn
  - override /bo: ucb
  - _self_

experiment_name: gap_dkl_vs_random
n_seeds: 30         # [P1] requires ≥20 seeds for statistically valid convergence curves
baselines: [random]
